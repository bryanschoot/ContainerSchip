namespace ContainerSchipNew.Models
{
    /// <summary>
    /// EEnum of the type of wieghts (for te select box)
    /// </summary>
    public enum Enum
    {
        Empty = 4000, //kg
        Full = 30000, //kg
    }
}